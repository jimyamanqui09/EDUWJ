CONNECT BOM_HORARIOS/123456@localhost/FREEPDB1

CREATE TABLE HORARIOS (
    ID              NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CURSO_ID        NUMBER        NOT NULL,
    DIA_SEMANA      VARCHAR2(10)  NOT NULL CHECK (DIA_SEMANA IN ('LUNES','MARTES','MIERCOLES','JUEVES','VIERNES','SABADO')),
    HORA_INICIO     VARCHAR2(5)   NOT NULL,
    HORA_FIN        VARCHAR2(5)   NOT NULL,
    AULA_ID         NUMBER        NOT NULL
);

ALTER TABLE HORARIOS ADD CONSTRAINT FK_HORARIOS_CURSO FOREIGN KEY (CURSO_ID) REFERENCES BOM_ACADEMICO.CURSOS(ID);
ALTER TABLE HORARIOS ADD CONSTRAINT FK_HORARIOS_AULA FOREIGN KEY (AULA_ID) REFERENCES BOM_ACADEMICO.AULAS(ID);

GRANT REFERENCES ON HORARIOS TO BOM_ASISTENCIA;

EXIT;
