CONNECT BOM_TAREAS/123456@localhost/FREEPDB1

CREATE TABLE TAREAS (
    ID              NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CURSO_ID        NUMBER        NOT NULL,
    TITULO          VARCHAR2(150) NOT NULL,
    DESCRIPCION     VARCHAR2(500),
    FECHA_ENTREGA   TIMESTAMP     NOT NULL,
    PESO            NUMBER(4,2)   DEFAULT 10.00 NOT NULL
);

CREATE TABLE ENTREGAS (
    ID              NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    TAREA_ID        NUMBER        NOT NULL,
    USUARIO_ID      NUMBER        NOT NULL,
    FECHA_ENTREGA   TIMESTAMP     DEFAULT SYSTIMESTAMP NOT NULL,
    URL_TRABAJO     VARCHAR2(300) NOT NULL,
    CALIFICACION    NUMBER(4,2),
    OBSERVACIONES   VARCHAR2(300),
    UNIQUE (TAREA_ID, USUARIO_ID)
);

ALTER TABLE TAREAS ADD CONSTRAINT FK_TAREAS_CURSO FOREIGN KEY (CURSO_ID) REFERENCES BOM_ACADEMICO.CURSOS(ID);
ALTER TABLE ENTREGAS ADD CONSTRAINT FK_ENT_TAREA FOREIGN KEY (TAREA_ID) REFERENCES TAREAS(ID);
ALTER TABLE ENTREGAS ADD CONSTRAINT FK_ENT_USER FOREIGN KEY (USUARIO_ID) REFERENCES BOM_SEGURIDAD.USUARIOS(ID);

EXIT;
