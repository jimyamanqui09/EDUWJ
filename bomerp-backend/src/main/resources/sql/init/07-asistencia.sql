CONNECT BOM_ASISTENCIA/123456@localhost/FREEPDB1

CREATE TABLE CLASES (
    ID              NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    HORARIO_ID      NUMBER        NOT NULL,
    FECHA           DATE          NOT NULL,
    TEMA            VARCHAR2(150)
);

CREATE TABLE ASISTENCIA (
    ID              NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CLASE_ID        NUMBER        NOT NULL,
    USUARIO_ID      NUMBER        NOT NULL,
    ESTADO          VARCHAR2(10)  NOT NULL CHECK (ESTADO IN ('PRESENTE','FALTA','TARDANZA')),
    UNIQUE (CLASE_ID, USUARIO_ID)
);

ALTER TABLE CLASES ADD CONSTRAINT FK_CLASE_HORARIO FOREIGN KEY (HORARIO_ID) REFERENCES BOM_HORARIOS.HORARIOS(ID);
ALTER TABLE ASISTENCIA ADD CONSTRAINT FK_ASIST_CLASE FOREIGN KEY (CLASE_ID) REFERENCES CLASES(ID);
ALTER TABLE ASISTENCIA ADD CONSTRAINT FK_ASIST_USER FOREIGN KEY (USUARIO_ID) REFERENCES BOM_SEGURIDAD.USUARIOS(ID);

EXIT;
