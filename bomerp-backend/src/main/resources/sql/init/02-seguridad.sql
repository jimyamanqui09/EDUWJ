CONNECT BOM_SEGURIDAD/123456@localhost/FREEPDB1

CREATE TABLE USUARIOS (
    ID              NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    DNI             VARCHAR2(8)   NOT NULL UNIQUE,
    NOMBRE          VARCHAR2(120) NOT NULL,
    EMAIL           VARCHAR2(120) NOT NULL UNIQUE,
    PASSWORD        VARCHAR2(200) NOT NULL,
    TIPO_USUARIO    VARCHAR2(20)  NOT NULL CHECK (TIPO_USUARIO IN ('ESTUDIANTE','DOCENTE','ADMIN')),
    ACTIVO          NUMBER(1)     DEFAULT 1 NOT NULL,
    FECHA_CREACION  TIMESTAMP     DEFAULT SYSTIMESTAMP NOT NULL
);

GRANT REFERENCES ON USUARIOS TO BOM_MATRICULAS;
GRANT REFERENCES ON USUARIOS TO BOM_TAREAS;
GRANT REFERENCES ON USUARIOS TO BOM_ASISTENCIA;
GRANT REFERENCES ON USUARIOS TO BOM_AVISOS;

EXIT;
