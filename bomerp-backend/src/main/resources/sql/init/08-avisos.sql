CONNECT BOM_AVISOS/123456@localhost/FREEPDB1

CREATE TABLE AVISOS (
    ID                  NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    TITULO              VARCHAR2(150) NOT NULL,
    CONTENIDO           VARCHAR2(1000) NOT NULL,
    FECHA_PUBLICACION   TIMESTAMP DEFAULT SYSTIMESTAMP NOT NULL,
    AUTOR_ID            NUMBER NOT NULL
);

ALTER TABLE AVISOS ADD CONSTRAINT FK_AVISO_AUTOR FOREIGN KEY (AUTOR_ID) REFERENCES BOM_SEGURIDAD.USUARIOS(ID);

EXIT;
