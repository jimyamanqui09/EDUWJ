CONNECT BOM_ACADEMICO/123456@localhost/FREEPDB1

CREATE TABLE CARRERAS (
    ID              NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODIGO          VARCHAR2(10)  NOT NULL UNIQUE,
    NOMBRE          VARCHAR2(120) NOT NULL,
    TOTAL_CREDITOS  NUMBER(3)     DEFAULT 0 NOT NULL
);

CREATE TABLE CICLOS (
    ID              NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CARRERA_ID      NUMBER        NOT NULL REFERENCES CARRERAS(ID),
    NUMERO          NUMBER(2)     NOT NULL,
    ANIO            NUMBER(4)     NOT NULL,
    SEMESTRE        NUMBER(1)     NOT NULL CHECK (SEMESTRE IN (1, 2)),
    UNIQUE (CARRERA_ID, NUMERO, ANIO, SEMESTRE)
);

CREATE TABLE AULAS (
    ID              NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE          VARCHAR2(30)  NOT NULL UNIQUE,
    CAPACIDAD       NUMBER(3)     DEFAULT 30 NOT NULL,
    EDIFICIO        VARCHAR2(60)
);

CREATE TABLE CURSOS (
    ID              NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CICLO_ID        NUMBER        NOT NULL REFERENCES CICLOS(ID),
    CARRERA_ID      NUMBER        NOT NULL REFERENCES CARRERAS(ID),
    NOMBRE          VARCHAR2(120) NOT NULL,
    CODIGO          VARCHAR2(10)  NOT NULL UNIQUE,
    CREDITOS        NUMBER(2)     NOT NULL,
    VACANTES        NUMBER(3)     DEFAULT 40 NOT NULL
);

GRANT REFERENCES ON CURSOS TO BOM_HORARIOS;
GRANT REFERENCES ON CURSOS TO BOM_MATRICULAS;
GRANT REFERENCES ON CURSOS TO BOM_TAREAS;
GRANT REFERENCES ON AULAS TO BOM_HORARIOS;

EXIT;
